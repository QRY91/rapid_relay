{% extends 'base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ discipline.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discipline_index') }}">back to list</a>

    <a href="{{ path('app_discipline_edit', {'id': discipline.id}) }}">edit</a>

    {{ include('discipline/_delete_form.html.twig') }}
{% endblock %}
